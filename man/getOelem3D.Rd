% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{getOelem3D}
\alias{getOelem3D}
\title{Map observation well (x, y, z) coordinates to flat element numbers (3D)}
\usage{
getOelem3D(grid, Oinf = data.frame(data = NA, x = 30.5, y = 30.5, z = 5.5))
}
\arguments{
\item{grid}{grid generated by \code{GenGrid3D}.}

\item{Oinf}{a data frame with columns \code{data}, \code{x}, \code{y}, \code{z}.}
}
\value{
\code{Oinf} with an additional column \code{nelem}.
}
\description{
Element numbering follows \code{expand.grid(x, y, z)}: x varies fastest.
For indices (ix, iy, iz): \code{nelem = (iz-1)*nx*ny + (iy-1)*nx + ix}.
}
\examples{
grid3d <- GenGrid3D()
Oinf <- data.frame(data = NA, x = 30.5, y = 30.5, z = 5.5)
getOelem3D(grid = grid3d, Oinf = Oinf)
}
