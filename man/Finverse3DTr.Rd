% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{Finverse3DTr}
\alias{Finverse3DTr}
\title{3D Transient Ensemble-based Bayesian inverse for hydraulic tomography (parallel)}
\usage{
Finverse3DTr(
  domain = c(15, 15, 5, 0, 15, 0, 15, 0, 5),
  grid = NULL,
  qHT = list(data.frame(Qp = 10, x = 7.5, y = 7.5, z = 2.5)),
  nsim = 50,
  itermax = 5,
  varmeanTmax = 5,
  rmsemin = 0,
  mul = 1,
  decay = 1.05,
  oHT = list(data.frame(data = -1, x = 5, y = 5, z = 2.5, time = 50)),
  Ss = 1e-04,
  times = seq(0, 100, by = 10),
  lrw = 2e+07,
  ncore = 4,
  geo = list(me = 0, var = 1, geomod = "Exp", range = 10, nugget = 0, anis = c(0, 0, 0,
    1, 1))
)
}
\arguments{
\item{domain}{9-element vector \code{c(nx, ny, nz, x1, x2, y1, y2, z1, z2)}.
\strong{Keep small for testing} (e.g. \code{c(15,15,5,0,15,0,15,0,5)}).}

\item{grid}{grid from \code{GenGrid3D()}; generated from \code{domain}
if \code{NULL}.}

\item{qHT}{list of pumping test data frames with columns
\code{Qp, x, y, z}.}

\item{nsim}{ensemble size (default 50; use ~20 for quick tests).}

\item{itermax}{maximum iterations (set to 1 for initial testing).}

\item{varmeanTmax}{convergence threshold on variance of mean ln(K).}

\item{rmsemin}{minimum RMSE stopping criterion.}

\item{mul}{stabiliser (default 1.0).}

\item{decay}{stabiliser decay per iteration (default 1.05).}

\item{oHT}{list of observation data frames with columns
\code{data, x, y, z, time}.}

\item{Ss}{specific storage Ss \code{[1/L]} — scalar or length-\code{n}
vector.  Default \code{1e-4}.}

\item{times}{numeric vector of output times for the ODE solver.  Must
cover all observation times in \code{oHT}.}

\item{lrw}{real work array length for \code{ode.3D}
(default 20000000; increase for larger grids).}

\item{ncore}{number of parallel cores (default 4).}

\item{geo}{prior geostatistical parameters — same list structure as
\code{random3d()}: \code{list(me, var, geomod, range, nugget, anis)}.
Note \code{anis} is a 5-element vector for 3D.}
}
\value{
list of per-iteration results, each a list with
\code{meanT} (mean ln K vector), \code{varT} (variance of ln K),
\code{meanobsh}, \code{varobsh}.
}
\description{
Transient extension of \code{Finverse3D}.  Replaces the steady-state forward
solver with \code{Ftransient3dsim} and uses \code{samData3DTr} for time-aware
observation sampling.  The Bayesian updating equations (ensemble smoother)
are identical to the steady-state version.
}
\examples{
# --- small synthetic test (15x15x5, 1 iteration) ---
domain3d <- c(15, 15, 5, 0, 15, 0, 15, 0, 5)
grid3d   <- GenGrid3D(domain3d)
set.seed(42)
trueK3d  <- random3d(nsim=1, grid=grid3d)
Qinf3d   <- data.frame(Qp=10, x=7.5, y=7.5, z=2.5)
qHT3d    <- list(test1 = Qinf3d)
times    <- seq(0, 1, by=0.05)
res3d    <- Ftransient3dsim(grid=grid3d, KK=trueK3d$Kp, Ss=1e-4,
                            Qinf=Qinf3d, times=times)
loc      <- expand.grid(x=c(3,6,9,12), y=c(3,6,9,12))
Oinf3d   <- data.frame(data=NA, x=loc$x, y=loc$y, z=2.5, time=0.5)
Oinf3d   <- samData3DTr(Oinf=Oinf3d, grid=grid3d, result_tr=res3d)
oHT3d    <- list(test1 = Oinf3d)
result3d <- Finverse3DTr(grid=grid3d, qHT=qHT3d, oHT=oHT3d,
                         Ss=1e-4, times=times, nsim=20, itermax=1, ncore=2)
}
