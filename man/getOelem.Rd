% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{getOelem}
\alias{getOelem}
\title{Map observation well (x, y) coordinates to flat element numbers}
\usage{
getOelem(
  domain = c(40, 40, 0, 40, 0, 40),
  grid = NULL,
  Oinf = data.frame(data = NA, x = 30.5, y = 30.5)
)
}
\arguments{
\item{domain}{a 6-element vector \code{c(nx, ny, x1, x2, y1, y2)}.  Ignored
when \code{grid} is supplied.}

\item{grid}{grid list from \code{GenGrid()}.}

\item{Oinf}{a data frame with columns \code{data} \code{[L]}, \code{x} \code{[L]},
\code{y} \code{[L]} giving observation drawdown and location.  \code{data} may
be \code{NA} for synthetic sampling.}
}
\value{
\code{Oinf} with an additional column \code{nelem} (flat element
number).
}
\description{
Element numbering follows \code{expand.grid(x, y)}: x varies fastest.
For indices (ix, iy): \code{nelem = (iy - 1) * nx + ix}.
}
\examples{
grid <- GenGrid()
Oinf=data.frame(data=NA,x=30.5,y=30.5)
Oinf <- getOelem(grid = grid, Oinf=Oinf)

}
