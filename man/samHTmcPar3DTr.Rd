% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTmcPar3DTr}
\alias{samHTmcPar3DTr}
\title{Parallel Monte Carlo forward runs for 3D transient HT (ensemble)}
\usage{
samHTmcPar3DTr(
  grid,
  KK = 0.1,
  Ss = 1e-04,
  qHT = list(data.frame(Qp = 10, x = 7.5, y = 7.5, z = 2.5)),
  oHT = list(data.frame(data = NA, x = 5, y = 5, z = 2.5, time = 50)),
  times = seq(0, 100, by = 10),
  lrw = 2e+07,
  ncore = 4
)
}
\arguments{
\item{grid}{grid from \code{GenGrid3D()}.}

\item{KK}{\code{n × nsim} matrix of K realisations.}

\item{Ss}{specific storage Ss \code{[1/L]} — scalar or length-\code{n}
vector.}

\item{qHT}{list of pumping test data frames (columns \code{Qp, x, y, z}).}

\item{oHT}{list of observation data frames (columns
\code{data, x, y, z, time}).}

\item{times}{numeric vector of output times for the ODE solver.}

\item{lrw}{real work array length; default 20000000.}

\item{ncore}{number of parallel cores.}
}
\value{
\code{nsim × nobs} matrix of simulated heads at observation wells.
}
\description{
Transient counterpart of \code{samHTmcPar3D}. Runs \code{samHT3Dtr} in
parallel across all ensemble members using \pkg{foreach} + \pkg{doParallel}.
}
\examples{
grid3d <- GenGrid3D(c(15,15,5,0,15,0,15,0,5))
KKmat  <- random3d(nsim=5, grid=grid3d)
KKmat  <- as.matrix(KKmat[,-c(1,2,3)])
Qinf   <- data.frame(Qp=10, x=7.5, y=7.5, z=2.5)
Oinf   <- data.frame(data=NA, x=c(5,10), y=c(7.5,7.5),
                     z=c(2.5,2.5), time=c(0.5, 0.5))
times  <- seq(0, 1, by=0.05)
da     <- samHTmcPar3DTr(grid=grid3d, KK=KKmat, Ss=1e-4,
                         qHT=list(Qinf), oHT=list(Oinf), times=times, ncore=2)
}
