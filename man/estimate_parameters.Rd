% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theis.R
\name{estimate_parameters}
\alias{estimate_parameters}
\title{Estimate aquifer parameters via Cooper-Jacob straight-line method}
\usage{
estimate_parameters(t, s, Q, r)
}
\arguments{
\item{t}{numeric vector of observed times \eqn{[T]}.}

\item{s}{numeric vector of observed drawdowns \eqn{[L]}.}

\item{Q}{pumping rate \eqn{[L^3/T]}.}

\item{r}{distance from pumping well \eqn{[L]}.}
}
\value{
list with components \code{T}, \code{S}, \code{a} (slope),
\code{t0} (intercept on log-time axis), \code{radius_influence},
and \code{lm_fit} (the \code{\link{lm}} object).
}
\description{
Fits a straight line to \eqn{s} vs \eqn{\log_{10}(t)} and computes
\eqn{T} and \eqn{S} from the slope and intercept.
}
\examples{
set.seed(123)
t_obs <- 10^seq(1.5, 4.5, length.out = 30)
s_true <- theis_drawdown(1.3e-3, 200, 1.5e-3, 2e-5, t_obs)
s_obs <- s_true + rnorm(length(s_true), 0, 0.02)
est <- estimate_parameters(t_obs, s_obs, Q = 1.3e-3, r = 200)
print(est$T)
print(est$S)
}
