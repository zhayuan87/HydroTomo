% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{covhkPlot2D}
\alias{covhkPlot2D}
\title{Plot cross-covariance (sensitivity) map for a single 2D observation}
\usage{
covhkPlot2D(
  covhk,
  grid,
  iobs = 1L,
  palette = "viridis",
  title = NULL,
  zlab = "Cov(h, lnT)",
  xlab = "X/m",
  ylab = "Y/m",
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 7
)
}
\arguments{
\item{covhk}{\code{nobs × n} cross-covariance matrix (heads × ln-T),
returned by a \code{Finverse*} function with \code{ifcor = TRUE}.}

\item{grid}{grid list from \code{GenGrid()}.}

\item{iobs}{integer; which observation row of \code{covhk} to plot
(1-based index into the concatenated observation vector).}

\item{palette}{\code{viridis} palette option (default \code{"viridis"}).}

\item{title}{plot title; auto-generated if \code{NULL}.}

\item{zlab}{colour-bar label; default \code{"Cov(h, lnT)"}.}

\item{xlab, ylab}{axis labels.}

\item{plotfile}{optional PDF file path.}

\item{plotwidth, plotheight}{PDF dimensions in inches.}
}
\value{
A \code{ggplot} object.
}
\description{
Given a \code{covhk} matrix returned by any parallel 2D inversion function
(e.g. \code{Finverse}, \code{FinverseTr}) with \code{ifcor = TRUE}, this
function plots the cross-covariance between one selected observation's head
and the log-transmissivity at every grid cell.  Use \code{+ geom_point()} /
\code{geom_text()} externally to overlay well locations.
}
\examples{
\dontrun{
covhk <- Finverse(grid = grid, qHT = qHT, oHT = oHT, ifcor = TRUE)
covhkPlot2D(covhk, grid, iobs = 1)
}
}
