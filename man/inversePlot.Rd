% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{inversePlot}
\alias{inversePlot}
\title{Visualise 2D inversion results (mean/variance maps + scatter diagnostics)}
\usage{
inversePlot(
  niterm = 1,
  grid,
  iterdf,
  oHT,
  trueK = NULL,
  p1title = "Est. lnT value",
  p1z = "lnT",
  p1xlab = "X/m",
  p1ylab = "Y/m",
  p2title = "Est. var. of lnT",
  p2z = "var. of lnT",
  p2xlab = "X/m",
  p2ylab = "Y/m",
  p3title = "Drawdown scatter",
  p3xlab = "Obs. Drawdown/m",
  p3ylab = "Sim. Drawdown/m",
  p4title = "lnT scatter",
  p4xlab = "True lnT /(m2/d)",
  p4ylab = "Est. lnT /(m2/d)"
)
}
\arguments{
\item{niterm}{iteration number to visualise (index into \code{iterdf}).}

\item{grid}{grid list from \code{GenGrid()}.}

\item{iterdf}{list returned by \code{Finverse()}.}

\item{oHT}{list of observation data frames used in the inversion (columns
\code{data, x, y}).}

\item{trueK}{(optional) the true transmissivity field from
\code{random2d()} — if supplied, a true-vs-estimated ln(T) scatter is
added as panel 4.}

\item{p1title, p1z, p1xlab, p1ylab}{labels for panel 1 (mean ln(T) map).}

\item{p2title, p2z, p2xlab, p2ylab}{labels for panel 2 (variance map).}

\item{p3title, p3xlab, p3ylab}{labels for panel 3 (drawdown scatter).}

\item{p4title, p4xlab, p4ylab}{labels for panel 4 (ln(T) scatter).}
}
\value{
A named list of ggplot objects: \code{lnk}, \code{varlnk},
\code{headscatter}, and (if \code{trueK} is provided) \code{lnkscatter}.
}
\description{
Creates a multi-panel diagnostic plot for a given iteration of the
ensemble-based inversion.  Panels 1 and 2 show the estimated ln(T) mean and
variance fields; panel 3 shows observed vs simulated drawdown; panel 4
(if \code{trueK} is provided) shows true vs estimated ln(T).
}
\examples{
set.seed(100)
trueK <- random2d(nsim=1)
TT <- trueK[,-c(1,2)]
domain=c(40,40,0,40,0,40)
grid = GenGrid(domain)
Qinf1=data.frame(Qp=10,xp=20.5,yp=20.5)
qHT <- list(test1 = Qinf1)
trueh <- Fsteady2dsim(TT=TT,Qinf=Qinf1,grid=grid)
trueh <- trueh$solution
locx = c(15,18,22,25,30)
locy = c(15,18,22,25,30)
loc= expand.grid(x=locx,y=locy)
Oinf <- data.frame(data=NA,x=loc$x,y=loc$y)
Oinf <- getOelem(grid = grid)
Oinf$data <- trueh[Oinf$nelem]
oHT <- list(test1 = Oinf)
result <- Finverse(grid =grid, qHT = qHT, oHT = oHT)
inversePlot(niterm=5,grid=grid,iterdf = result,oHT = oHT,trueK=trueK)
}
