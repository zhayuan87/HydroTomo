% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTmcParTr}
\alias{samHTmcParTr}
\title{Parallel Monte Carlo forward runs for 2D transient HT (ensemble)}
\usage{
samHTmcParTr(
  grid,
  TT = 0.1,
  SS = 1e-04,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  oHT = list(data.frame(data = NA, x = 11, y = 11, time = 50)),
  times = seq(0, 100, by = 10),
  lrw = 1600000,
  ncore = 4
)
}
\arguments{
\item{grid}{grid from \code{GenGrid()}.}

\item{TT}{\code{n × nsim} matrix of T realisations.}

\item{SS}{storage coefficient S \link{-} — scalar or length-\code{n} vector.}

\item{qHT}{list of pumping test data frames (columns \code{Qp, x, y}).}

\item{oHT}{list of observation data frames (columns \code{data, x, y, time}).}

\item{times}{numeric vector of output times for the ODE solver.}

\item{lrw}{real work array length; default 1600000.}

\item{ncore}{number of parallel cores.}
}
\value{
\code{nsim × nobs} matrix of simulated heads at observation wells.
}
\description{
Transient counterpart of \code{samHTmcPar}. Runs \code{samHTtr} in parallel
across all ensemble members using \pkg{foreach} + \pkg{doParallel}.
}
\examples{
grid  <- GenGrid()
TTmat <- random2d(nsim=5, grid=grid)
TTmat <- as.matrix(TTmat[,-c(1,2)])
Qinf  <- data.frame(Qp=10, x=20.5, y=20.5)
Oinf  <- data.frame(data=NA, x=c(15,25), y=c(20,20), time=c(0.5, 0.5))
times <- seq(0, 1, by=0.05)
da    <- samHTmcParTr(grid=grid, TT=TTmat, SS=1e-4,
                      qHT=list(Qinf), oHT=list(Oinf), times=times, ncore=2)
}
