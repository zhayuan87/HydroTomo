% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTtr}
\alias{samHTtr}
\title{Run forward simulations for one T field across all 2D transient pumping tests}
\usage{
samHTtr(
  grid,
  TT = 0.1,
  SS = 1e-04,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  oHT = list(data.frame(data = NA, x = 11, y = 11, time = 50)),
  times = seq(0, 100, by = 10),
  lrw = 1600000,
  simplify = TRUE
)
}
\arguments{
\item{grid}{grid from \code{GenGrid()}.}

\item{TT}{length-\code{n} vector of transmissivity T \code{[L^2/T]}.}

\item{SS}{storage coefficient S \link{-} — scalar or length-\code{n} vector.}

\item{qHT}{list of pumping test data frames, each with columns
\code{Qp, x, y}.}

\item{oHT}{list of observation data frames, each with columns
\code{data, x, y, time}.}

\item{times}{numeric vector of output times for the ODE solver.  Must
include all observation times in \code{oHT}.}

\item{lrw}{real work array length for \code{ode.2D}; default 1600000.}

\item{simplify}{if \code{TRUE} return a plain vector of sampled heads
(all tests concatenated); if \code{FALSE} return updated \code{oHT}.}
}
\value{
vector of sampled drawdown values (when \code{simplify = TRUE}).
}
\description{
Transient counterpart of \code{samHT}. Calls \code{Ftransient2dsim} for each
pumping test and samples simulated heads at observation well locations and
times using \code{samDataTr}.
}
\examples{
grid  <- GenGrid()
Qinf  <- data.frame(Qp=10, x=20.5, y=20.5)
Oinf  <- data.frame(data=NA, x=c(15,25), y=c(20,20), time=c(0.5, 0.5))
times <- seq(0, 1, by=0.05)
da    <- samHTtr(grid=grid, TT=0.1, SS=1e-4,
                 qHT=list(Qinf), oHT=list(Oinf), times=times)
}
