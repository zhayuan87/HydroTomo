% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTmcPar3D}
\alias{samHTmcPar3D}
\title{Parallel Monte Carlo forward runs for 3D HT (ensemble)}
\usage{
samHTmcPar3D(
  grid,
  TT = 0.1,
  qHT = list(data.frame(Qp = 10, x = 10.5, y = 10.5, z = 2.5)),
  oHT = list(data.frame(data = NA, x = 5, y = 5, z = 2.5)),
  lrw = 2e+07,
  ncore = 4
)
}
\arguments{
\item{grid}{grid from \code{GenGrid3D()}.}

\item{TT}{\code{n × nsim} matrix of K realisations.}

\item{qHT}{list of pumping test data frames (columns \code{Qp, x, y, z}).}

\item{oHT}{list of observation data frames (columns \code{data, x, y, z}).}

\item{lrw}{real work array length; default 20000000.}

\item{ncore}{number of parallel cores.}
}
\value{
\code{nsim × nobs} matrix of simulated heads at observation wells.
}
\description{
3D counterpart of \code{samHTmcPar}. Runs \code{samHT3D} in parallel across
all ensemble members.
}
\examples{
grid3d <- GenGrid3D(c(15,15,5,0,15,0,15,0,5))
KK     <- random3d(nsim=5, grid=grid3d)
TT     <- as.matrix(KK[,-c(1,2,3)])
Qinf   <- data.frame(Qp=10, x=7.5, y=7.5, z=2.5)
Oinf   <- data.frame(data=NA, x=c(10,12), y=c(7.5,7.5), z=c(2.5,2.5))
da     <- samHTmcPar3D(grid=grid3d, TT=TT,
                       qHT=list(Qinf), oHT=list(Oinf), ncore=2)
}
