% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHT3Dtr}
\alias{samHT3Dtr}
\title{Run forward simulations for one K field across all 3D transient pumping tests}
\usage{
samHT3Dtr(
  grid,
  KK = 0.1,
  Ss = 1e-04,
  qHT = list(data.frame(Qp = 10, x = 7.5, y = 7.5, z = 2.5)),
  oHT = list(data.frame(data = NA, x = 5, y = 5, z = 2.5, time = 50)),
  times = seq(0, 100, by = 10),
  lrw = 2e+07,
  simplify = TRUE
)
}
\arguments{
\item{grid}{grid from \code{GenGrid3D()}.}

\item{KK}{length-\code{n} vector of hydraulic conductivity K \code{[L/T]}.}

\item{Ss}{specific storage Ss \code{[1/L]} — scalar or length-\code{n}
vector.}

\item{qHT}{list of pumping test data frames, each with columns
\code{Qp, x, y, z}.}

\item{oHT}{list of observation data frames, each with columns
\code{data, x, y, z, time}.}

\item{times}{numeric vector of output times for the ODE solver.  Must
include all observation times in \code{oHT}.}

\item{lrw}{real work array length for \code{ode.3D}; default 20000000.}

\item{simplify}{if \code{TRUE} return a plain vector of sampled heads
(all tests concatenated); if \code{FALSE} return updated \code{oHT}.}
}
\value{
vector of sampled drawdown values (when \code{simplify = TRUE}).
}
\description{
Transient counterpart of \code{samHT3D}. Calls \code{Ftransient3dsim} for each
pumping test and samples simulated heads at observation well locations and
times using \code{samData3DTr}.
}
\examples{
grid3d <- GenGrid3D(c(15, 15, 5, 0, 15, 0, 15, 0, 5))
Qinf   <- data.frame(Qp=10, x=7.5, y=7.5, z=2.5)
Oinf   <- data.frame(data=NA, x=c(5,10), y=c(7.5,7.5),
                     z=c(2.5,2.5), time=c(0.5, 0.5))
times  <- seq(0, 1, by=0.05)
da     <- samHT3Dtr(grid=grid3d, KK=0.1, Ss=1e-4,
                    qHT=list(Qinf), oHT=list(Oinf), times=times)
}
