% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHT3DScreen}
\alias{samHT3DScreen}
\title{Run forward simulations for one K field — well-screen version (3D steady)}
\usage{
samHT3DScreen(
  grid,
  TT = 0.1,
  qHT = list(data.frame(Qp = 10, x = 7.5, y = 7.5, z_top = 2, z_bottom = 3)),
  oHT = list(data.frame(data = NA, x = 5, y = 5, z_top = 1, z_bottom = 4)),
  lrw = 2e+07,
  simplify = TRUE
)
}
\arguments{
\item{grid}{grid from \code{GenGrid3D()}.}

\item{TT}{length-\code{n} vector of hydraulic conductivity K \code{[L/T]}.}

\item{qHT}{list of pumping test data frames, each with columns
\code{Qp, x, y, z_top, z_bottom} (well-screen pumping) or
\code{Qp, x, y, z} (point pumping).}

\item{oHT}{list of observation data frames, each with columns
\code{data, x, y, z_top, z_bottom}.}

\item{lrw}{real work array length for \code{steady.3D}; default 20000000.}

\item{simplify}{if \code{TRUE} return a plain vector of sampled heads
(all tests concatenated); if \code{FALSE} return updated \code{oHT}.}
}
\value{
vector of interval-averaged drawdown values (when \code{simplify = TRUE}).
}
\description{
Steady-state counterpart of \code{\link{samHT3DtrScreen}}.  Calls
\code{Fsteady3dsim} for each pumping test and averages simulated heads
over the vertical screen interval of each observation well.
}
\examples{
grid3d <- GenGrid3D(c(15,15,5,0,15,0,15,0,5))
Qinf   <- data.frame(Qp=10, x=7.5, y=7.5, z_top=2, z_bottom=3)
Oinf   <- data.frame(data=NA, x=c(5,10), y=c(7.5,7.5),
                     z_top=c(1,1), z_bottom=c(4,4))
da     <- samHT3DScreen(grid=grid3d, TT=0.1,
                        qHT=list(Qinf), oHT=list(Oinf))
}
