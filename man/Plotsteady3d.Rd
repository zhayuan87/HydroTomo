% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{Plotsteady3d}
\alias{Plotsteady3d}
\title{Plot 3D steady-state head or drawdown field from Fsteady3dsim()}
\usage{
Plotsteady3d(
  s3,
  grid,
  ifdrawdown = TRUE,
  type = "slice",
  xs = NULL,
  ys = NULL,
  zs = NULL,
  level = NULL,
  palette = "jet",
  Qinf = NULL,
  Oinf = NULL,
  theta = 40,
  phi = 25,
  title = NULL,
  clab = NULL,
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 7,
  ...
)
}
\arguments{
\item{s3}{data frame returned by \code{Fsteady3dsim()} (columns
\code{x, y, z, solution}).}

\item{grid}{grid object from \code{GenGrid3D()}.}

\item{ifdrawdown}{logical; if \code{TRUE} (default) negate the solution to
display drawdown (positive values = water-level decline).}

\item{type}{character, one of \code{"slice"} (default) or
\code{"isosurf"}.}

\item{xs, ys, zs}{slice-plane positions for \code{type = "slice"}.
Default: midpoint of each axis.}

\item{level}{isosurface level for \code{type = "isosurf"}.  Default: 25th
percentile of non-zero values.}

\item{palette}{character; colour palette — \code{"jet"} (default),
\code{"jet2"}, \code{"gg"}, or \code{"heat"}.}

\item{Qinf}{data frame with columns \code{x, y, z} for pumping wells
(plotted as red filled circles).  \code{NULL} to omit.}

\item{Oinf}{data frame with columns \code{x, y, z} for observation wells
(plotted as yellow filled squares).  \code{NULL} to omit.}

\item{theta, phi}{viewing angles (degrees).}

\item{title, clab}{plot title and colour-key label.}

\item{plotfile}{optional PDF output path.}

\item{plotwidth, plotheight}{PDF dimensions in inches.}

\item{...}{additional arguments forwarded to the \pkg{plot3D} function.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Visualises the 3D head/drawdown field produced by
\code{\link{Fsteady3dsim}} using the \pkg{plot3D} package.  Pumping well
and observation well locations can be overlaid.
}
\examples{
grid3d <- GenGrid3D()
s3     <- Fsteady3dsim(grid = grid3d)
Plotsteady3d(s3, grid3d)
Qinf   <- data.frame(Qp = 10, x = 20.5, y = 20.5, z = 5.5)
Plotsteady3d(s3, grid3d, Qinf = Qinf, type = "isosurf")
}
