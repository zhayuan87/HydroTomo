% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{PlotTr2d}
\alias{PlotTr2d}
\title{Plot time-drawdown curves for 2D transient flow simulation}
\usage{
PlotTr2d(
  result_tr,
  grid,
  loc,
  label = NULL,
  ifdrawdown = TRUE,
  iflogx = FALSE,
  title = NULL,
  xlab = "Time",
  ylab = NULL,
  palette = "viridis",
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 5,
  linewidth = 1
)
}
\arguments{
\item{result_tr}{list returned by \code{\link{Ftransient2dsim}}.}

\item{grid}{grid list from \code{\link{GenGrid}}.}

\item{loc}{a data frame with columns \code{x} and \code{y} giving the
coordinates of observation points.  One curve is drawn per row.}

\item{label}{optional character vector of legend labels, one per
location.  If \code{NULL}, labels are auto-generated as
\code{"(x, y)"}.}

\item{ifdrawdown}{logical; if \code{TRUE} (default), negate head to show
drawdown (positive = water-level decline).}

\item{iflogx}{logical; if \code{TRUE}, use log10 scale for the time axis.}

\item{title}{plot title.  Auto-set if \code{NULL}.}

\item{xlab, ylab}{axis labels (default \code{"Time"} and \code{"Drawdown / m"}
or \code{"Head / m"}).}

\item{palette}{character; \code{viridis} palette option for colour mapping.}

\item{plotfile}{optional file path; if supplied the plot is saved as a PDF.}

\item{plotwidth, plotheight}{PDF dimensions in inches.}

\item{linewidth}{line width for the curves (default 1).}
}
\value{
A \code{ggplot} object.
}
\description{
Given the output of \code{\link{Ftransient2dsim}} and one or more
observation locations \code{(x, y)}, extracts the head time-series at each
location and plots the drawdown (or head) evolution over time.
}
\examples{
grid  <- GenGrid()
res   <- Ftransient2dsim(grid = grid, times = seq(0, 1, by = 0.05))
loc   <- data.frame(x = c(10.5, 20.5, 30.5), y = c(20.5, 15.5, 25.5))
PlotTr2d(res, grid, loc)
PlotTr2d(res, grid, loc, label = c("Well A", "Well B", "Well C"), iflogx = TRUE)
}
