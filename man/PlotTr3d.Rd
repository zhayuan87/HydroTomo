% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{PlotTr3d}
\alias{PlotTr3d}
\title{Plot time-drawdown curves for 3D transient flow simulation}
\usage{
PlotTr3d(
  result_tr,
  grid,
  loc,
  label = NULL,
  ifdrawdown = TRUE,
  iflogx = FALSE,
  title = NULL,
  xlab = "Time",
  ylab = NULL,
  palette = "viridis",
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 5,
  linewidth = 1
)
}
\arguments{
\item{result_tr}{list returned by \code{\link{Ftransient3dsim}}.}

\item{grid}{grid list from \code{\link{GenGrid3D}}.}

\item{loc}{a data frame with columns \code{x}, \code{y}, \code{z}
giving the coordinates of observation points.  One curve per row.}

\item{label}{optional character vector of legend labels, one per
location.  If \code{NULL}, labels are auto-generated as
\code{"(x, y, z)"}.}

\item{ifdrawdown}{logical; if \code{TRUE} (default), negate head to show
drawdown (positive = water-level decline).}

\item{iflogx}{logical; if \code{TRUE}, use log10 scale for the time axis.}

\item{title}{plot title.  Auto-set if \code{NULL}.}

\item{xlab, ylab}{axis labels (default \code{"Time"} and \code{"Drawdown / m"}
or \code{"Head / m"}).}

\item{palette}{character; \code{viridis} palette option for colour mapping.}

\item{plotfile}{optional file path; if supplied the plot is saved as a PDF.}

\item{plotwidth, plotheight}{PDF dimensions in inches.}

\item{linewidth}{line width for the curves (default 1).}
}
\value{
A \code{ggplot} object.
}
\description{
Given the output of \code{\link{Ftransient3dsim}} and one or more
observation locations \code{(x, y, z)}, extracts the head time-series at
each location and plots the drawdown (or head) evolution over time.
}
\examples{
grid3d <- GenGrid3D()
res3d  <- Ftransient3dsim(grid = grid3d, times = seq(0, 1, by = 0.05))
loc    <- data.frame(x = c(10.5, 25.5), y = c(20.5, 20.5), z = c(5.5, 5.5))
PlotTr3d(res3d, grid3d, loc)
PlotTr3d(res3d, grid3d, loc, label = c("Shallow", "Deep"), iflogx = TRUE)
}
