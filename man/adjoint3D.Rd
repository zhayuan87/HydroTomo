% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{adjoint3D}
\alias{adjoint3D}
\title{Solve the adjoint equation for a single observation point (3D steady-state)}
\usage{
adjoint3D(grid, KK, iobs, lrw = 2e+07)
}
\arguments{
\item{grid}{grid list from \code{GenGrid3D()}.}

\item{KK}{hydraulic conductivity field \eqn{K} \code{[L/T]}, length-\code{n} vector.}

\item{iobs}{scalar integer, the flat element index of the observation point.}

\item{lrw}{real work array length for \code{steady.3D} (default 20000000).}
}
\value{
Numeric vector of adjoint variable \eqn{\lambda} (length \code{n}).
}
\description{
3D counterpart of \code{\link{adjoint2D}}.  For the 3D steady-state
groundwater flow equation \eqn{\nabla \cdot (K \nabla h) = Q}, the adjoint
state \eqn{\lambda} for an observation at cell \code{iobs} satisfies:
\deqn{\nabla \cdot (K \nabla \lambda) = \delta(\mathbf{x} - \mathbf{x}_{iobs})}
with homogeneous (zero) boundary conditions.  The adjoint variable is used
to compute the Jacobian (sensitivity) matrix via:
\deqn{\frac{\partial h_{iobs}}{\partial (\ln K)_k} =
      -K_k \, \nabla h|_k \cdot \nabla \lambda|_k \, \Delta x \, \Delta y \, \Delta z}
}
\keyword{internal}
