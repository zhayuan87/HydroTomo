% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHT}
\alias{samHT}
\title{Run forward simulations for one T field across all 2D pumping tests}
\usage{
samHT(
  domain = c(40, 40, 0, 40, 0, 40),
  grid = NULL,
  TT = 0.1,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  oHT = list(data.frame(data = -1, x = 11, y = 11)),
  lrw = 160000,
  simplify = TRUE
)
}
\arguments{
\item{domain}{a 6-element vector \code{c(nx, ny, x1, x2, y1, y2)} \code{[L]}.}

\item{grid}{grid list from \code{GenGrid()}.}

\item{TT}{transmissivity \eqn{T} \code{[L\eqn{^2}/T]} — either a scalar or a
length-\code{n} vector (one value per cell).}

\item{qHT}{list of pumping test data frames, each with columns \code{Qp}
\code{[L\eqn{^3}/T]}, \code{x} \code{[L]}, \code{y} \code{[L]}.}

\item{oHT}{list of observation data frames, each with columns \code{data}
\code{[L]}, \code{x} \code{[L]}, \code{y} \code{[L]}.}

\item{lrw}{real work array length for \code{steady.2D} (default 160000).}

\item{simplify}{if \code{TRUE} (default) return a plain vector of sampled
heads concatenated across all tests; if \code{FALSE} return the updated
\code{oHT} list.}
}
\value{
A numeric vector of sampled drawdown values (when
\code{simplify = TRUE}), or the updated \code{oHT} list (when
\code{simplify = FALSE}).
}
\description{
For a single transmissivity field, runs \code{Fsteady2dsim} for each pumping
test in \code{qHT} and samples the simulated heads at the observation
locations given in \code{oHT}.  Used internally by the inversion functions.
}
\examples{
domain=c(80,80,0,80,0,80)
grid <- GenGrid(domain=domain)
TT =0.1
data("oHT")
data("qHT")
da = samHT(grid =grid, TT=TT, qHT= qHT, oHT=oHT,lrw=320000)
}
