% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{covhkPlot3D}
\alias{covhkPlot3D}
\title{Plot cross-covariance (sensitivity) slice for a single 3D observation}
\usage{
covhkPlot3D(
  covhk,
  grid,
  iobs = 1L,
  zslice = NULL,
  palette = "viridis",
  title = NULL,
  zlab = "Cov(h, lnK)",
  xlab = "X/m",
  ylab = "Y/m",
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 7
)
}
\arguments{
\item{covhk}{\code{nobs × n} cross-covariance matrix.}

\item{grid}{grid from \code{GenGrid3D()}.}

\item{iobs}{integer; which observation row to plot.}

\item{zslice}{z coordinate \code{[L]} of the horizontal slice to display.
Default: middle layer of the grid.}

\item{palette}{\code{viridis} palette option.}

\item{title}{plot title.}

\item{zlab}{colour-bar label.}

\item{xlab, ylab}{axis labels.}

\item{plotfile}{optional PDF path.}

\item{plotwidth, plotheight}{PDF dimensions.}
}
\value{
A \code{ggplot} object.
}
\description{
3D counterpart of \code{\link{covhkPlot2D}}.  Given a \code{covhk} matrix
from \code{Finverse3D}, \code{Finverse3DTr}, or \code{Finverse3DTrScreen}
(all with \code{ifcor = TRUE}), this function plots a horizontal z-slice of
the cross-covariance between one observation's head and log-K at every grid
cell.  Use \code{+ geom_point()} / \code{geom_text()} externally to overlay
well locations.
}
\examples{
\dontrun{
covhk <- Finverse3D(grid = grid3d, qHT = qHT, oHT = oHT, ifcor = TRUE)
covhkPlot3D(covhk, grid3d, iobs = 1)
}
}
