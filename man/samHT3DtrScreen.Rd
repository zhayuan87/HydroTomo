% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHT3DtrScreen}
\alias{samHT3DtrScreen}
\title{Run forward simulations for one K field — 3D transient well-screen version}
\usage{
samHT3DtrScreen(
  grid,
  KK = 0.1,
  Ss = 1e-04,
  qHT = list(data.frame(Qp = 10, x = 7.5, y = 7.5, z_top = 2, z_bottom = 3)),
  oHT = list(data.frame(data = NA, x = 5, y = 5, z_top = 1, z_bottom = 4, time = 50)),
  times = seq(0, 100, by = 10),
  lrw = 2e+07,
  simplify = TRUE
)
}
\arguments{
\item{grid}{grid from \code{GenGrid3D()}.}

\item{KK}{length-\code{n} vector of hydraulic conductivity K \code{[L/T]}.}

\item{Ss}{specific storage Ss \code{[1/L]} — scalar or length-\code{n}
vector.}

\item{qHT}{list of pumping test data frames, each with columns
\code{Qp, x, y, z_top, z_bottom} (well-screen pumping) or
\code{Qp, x, y, z} (point pumping).}

\item{oHT}{list of observation data frames, each with columns
\code{data, x, y, z_top, z_bottom, time}.}

\item{times}{numeric vector of output times for the ODE solver.}

\item{lrw}{real work array length; default 20000000.}

\item{simplify}{if \code{TRUE} return a plain vector; \code{FALSE} returns
updated \code{oHT}.}
}
\value{
vector of interval-averaged drawdown values (when \code{simplify = TRUE}).
}
\description{
Like \code{\link{samHT3Dtr}} but for wells with vertical screens.
Calls \code{\link{Ftransient3dsim}} and samples interval-averaged heads
via \code{\link{samData3DTrScreen}}.
}
\examples{
grid3d <- GenGrid3D(c(15, 15, 5, 0, 15, 0, 15, 0, 5))
Qinf   <- data.frame(Qp=10, x=7.5, y=7.5, z_top=2, z_bottom=3)
Oinf   <- data.frame(data=NA, x=c(5,10), y=c(7.5,7.5),
                     z_top=c(1,1), z_bottom=c(4,4), time=c(0.5, 0.5))
times  <- seq(0, 1, by=0.05)
da     <- samHT3DtrScreen(grid=grid3d, KK=0.1, Ss=1e-4,
                          qHT=list(Qinf), oHT=list(Oinf), times=times)
}
