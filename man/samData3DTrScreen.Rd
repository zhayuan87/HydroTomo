% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transient3D.R
\name{samData3DTrScreen}
\alias{samData3DTrScreen}
\title{Sample transient 3D head values at well-screen intervals}
\usage{
samData3DTrScreen(Oinf, grid, result_tr)
}
\arguments{
\item{Oinf}{data frame with columns \code{data}, \code{x}, \code{y},
\code{z_top}, \code{z_bottom}, \code{time}.}

\item{grid}{grid from \code{\link{GenGrid3D}}.}

\item{result_tr}{list returned by \code{\link{Ftransient3dsim}}.}
}
\value{
\code{Oinf} with the \code{data} column filled with
interval-averaged heads.
}
\description{
Transient counterpart of \code{\link{samData3DScreen}}.  For each
observation well with a vertical screen defined by \code{z_top} and
\code{z_bottom}, computes the arithmetic mean of simulated heads at the
requested observation time over all grid cells within the screen interval.
}
\examples{
grid3d <- GenGrid3D(c(15, 15, 5, 0, 15, 0, 15, 0, 5))
times  <- c(0, 10, 50, 100)
res3d  <- Ftransient3dsim(grid = grid3d, times = times)
Oinf   <- data.frame(data = NA,
                     x      = c(5.5, 10.5),
                     y      = c(7.5,  7.5),
                     z_top  = c(1.0,  1.0),
                     z_bottom = c(4.0, 4.0),
                     time   = c(50, 100))
Oinf   <- samData3DTrScreen(Oinf = Oinf, grid = grid3d, result_tr = res3d)
}
