% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTmc}
\alias{samHTmc}
\title{Serial Monte Carlo forward runs for 2D HT (ensemble)}
\usage{
samHTmc(
  domain = c(40, 40, 0, 40, 0, 40),
  grid = NULL,
  TT = 0.1,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  oHT = list(data.frame(data = -1, x = 11, y = 11)),
  lrw = 160000
)
}
\arguments{
\item{domain}{a 6-element vector \code{c(nx, ny, x1, x2, y1, y2)} \code{[L]}.}

\item{grid}{grid list from \code{GenGrid()}.}

\item{TT}{\code{n × nsim} matrix of transmissivity realisations \code{[L\eqn{^2}/T]}.}

\item{qHT}{list of pumping test data frames (\code{Qp, x, y}).}

\item{oHT}{list of observation data frames (\code{data, x, y}).}

\item{lrw}{real work array length for \code{steady.2D} (default 160000).}
}
\value{
An \code{nsim × nobs} matrix of simulated heads at observation wells.
}
\description{
Runs \code{samHT} for each ensemble member using \pkg{foreach}
(\code{\%do\%}, serial).  Returns a matrix where each row corresponds to
one ensemble member and columns are observed heads concatenated across all
pumping tests.
}
\examples{
domain=c(80,80,0,80,0,80)
grid <- GenGrid(domain=domain)
TT= random2d(nsim=10,grid=grid)
TT = TT[,-c(1,2)]
data("oHT")
data("qHT")
da = samHTmc(grid =grid, TT=TT, qHT= qHT, oHT=oHT,lrw=320000)
}
