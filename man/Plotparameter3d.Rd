% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HydroPlot.R
\name{Plotparameter3d}
\alias{Plotparameter3d}
\title{Plot 3D hydraulic conductivity field from random3d()}
\usage{
Plotparameter3d(
  KK,
  grid,
  iflog = TRUE,
  type = "slice",
  xs = NULL,
  ys = NULL,
  zs = NULL,
  level = NULL,
  palette = "jet",
  theta = 40,
  phi = 25,
  title = NULL,
  clab = NULL,
  plotfile = NULL,
  plotwidth = 8,
  plotheight = 7,
  ...
)
}
\arguments{
\item{KK}{data frame returned by \code{random3d()} (columns
\code{x, y, z, Kp} or \code{sim1, …}).  If \code{nsim > 1} was used,
the first simulation column is plotted.}

\item{grid}{grid object from \code{GenGrid3D()}.}

\item{iflog}{logical; if \code{TRUE} (default) plot \eqn{\ln K}, otherwise
plot \eqn{K} directly.}

\item{type}{character, one of \code{"slice"} (default), \code{"isosurf"},
or \code{"voxel"}.}

\item{xs, ys, zs}{positions of the slice planes (passed to \code{slice3D}).
Default: midpoint of each axis.  Ignored for \code{type = "isosurf"} /
\code{"voxel"}.}

\item{level}{isosurface level for \code{type = "isosurf"}.  Default: median
of the plotted variable.}

\item{palette}{character; colour palette: \code{"jet"} (default),
\code{"jet2"}, \code{"gg"}, or \code{"heat"}.}

\item{theta, phi}{viewing angles (degrees) passed to \pkg{plot3D}.}

\item{title, clab}{plot title and colour-key label.  Sensible defaults are
set automatically.}

\item{plotfile}{optional file path; if supplied the plot is saved as a PDF.}

\item{plotwidth, plotheight}{PDF dimensions in inches.}

\item{...}{additional arguments forwarded to the underlying \pkg{plot3D}
function.}
}
\value{
Invisibly returns \code{NULL}; the plot is drawn as a side-effect.
}
\description{
Visualises the 3D hydraulic conductivity (or log-K) field generated by
\code{\link{random3d}} using the \pkg{plot3D} package.  Three display types
are supported: mutually-perpendicular slices, isosurfaces, and a voxel plot.
}
\examples{
grid3d <- GenGrid3D()
KK     <- random3d(nsim = 1, grid = grid3d)
Plotparameter3d(KK, grid3d)
Plotparameter3d(KK, grid3d, type = "isosurf")
}
