% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{FinverseAdjTr}
\alias{FinverseAdjTr}
\title{2D Transient adjoint-based deterministic inversion for hydraulic tomography}
\usage{
FinverseAdjTr(
  domain = c(40, 40, 0, 40, 0, 40),
  grid = NULL,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  itermax = 5,
  rmsemin = 0,
  oHT = list(data.frame(data = -1, x = 11, y = 11, time = 50)),
  SS = 1e-04,
  times = NULL,
  npoints = 151,
  p_grid = 1.03,
  nconv = 201,
  lrw = 1600000,
  sigma2obs = 1e-04,
  lambda_lm = 1,
  decay_lm = 2,
  geo = list(me = 0, var = 1, geomod = "Exp", anis = c(90, 1), range = 30, nugget = 0),
  m0 = NULL,
  ncore = 1,
  ifcor = FALSE,
  ifvarTh = FALSE
)
}
\arguments{
\item{domain}{a 6-element vector \code{c(nx, ny, x1, x2, y1, y2)} \code{[L]}.}

\item{grid}{grid list from \code{GenGrid()}; generated from \code{domain}
if \code{NULL}.}

\item{qHT}{list of pumping test data frames (columns \code{Qp, x, y}).}

\item{itermax}{maximum number of iterations (default 5).}

\item{rmsemin}{minimum RMSE stopping criterion (default 0).}

\item{oHT}{list of observation data frames (columns \code{data, x, y, time}).}

\item{SS}{storage coefficient \eqn{S} \link{-} — scalar or length-\code{n}
vector (default \code{1e-4}).}

\item{times}{numeric vector of output times for the ODE solver.
If \code{NULL} (default), a geometric grid is auto-generated.}

\item{npoints}{number of time points for auto-generated grids (default 151).}

\item{p_grid}{growth factor for auto-generated time grids (default 1.03).}

\item{nconv}{number of points on the common convolution grid (default 201).}

\item{lrw}{real work array length for \code{ode.2D} (default 1600000).}

\item{sigma2obs}{observation error variance (default 1e-4).}

\item{lambda_lm}{Levenberg-Marquardt damping parameter (default 1.0); decays
by \code{decay_lm} each iteration.}

\item{decay_lm}{decay factor for \code{lambda_lm} (default 2.0).}

\item{geo}{prior geostatistical parameters:
\code{list(me, var, geomod, anis, range, nugget)}.}

\item{m0}{initial log-T field, length \code{n} vector.  If \code{NULL}
(default), uses the prior mean \code{geo$me} everywhere.}

\item{ncore}{number of CPU cores for parallel adjoint solves (default 1).
When \code{ncore > 1} and there are multiple observation wells, the
inner loop of \code{\link{jacobian2D}} (adjoint solve per observation)
is parallelized via \pkg{foreach} + \pkg{doParallel}.}

\item{ifcor}{logical; if \code{TRUE}, returns the Jacobian matrix and
forward response after the first iteration without performing the update.}

\item{ifvarTh}{logical; if \code{TRUE}, computes and stores \code{varobsh}
and \code{varT}.  Default \code{FALSE}.}
}
\value{
A list of per-iteration results (same structure as \code{\link{FinverseAdj}}).
}
\description{
Transient counterpart of \code{\link{FinverseAdj}}.  Performs deterministic
Bayesian inversion to estimate the 2D log-transmissivity field
\eqn{\ln T(\mathbf{x})} from transient hydraulic tomography (HT) data using
the \strong{adjoint-state method} to compute the Jacobian (sensitivity) matrix
analytically via \code{\link{jacobian2DTr}}.
}
\details{
The update follows the same quasi-linear Bayesian formulation as
\code{\link{FinverseAdj}}, with forward simulations handled by
\code{\link{Ftransient2dsim}} and observations sampled via
\code{\link{samDataTr}}.
}
\examples{
\dontrun{
set.seed(100)
trueK <- random2d(nsim=1)
TT <- trueK$Tp
grid <- GenGrid(c(40,40,0,40,0,40))
Qinf1 <- data.frame(Qp=10, x=20.5, y=20.5)
qHT <- list(test1 = Qinf1)
loc <- expand.grid(x=c(15,18,22,25,30), y=c(15,18,22,25,30))
Oinf1 <- data.frame(data=NA, x=loc$x, y=loc$y, time=50)
res_fwd <- Ftransient2dsim(grid=grid, TT=TT, Qinf=Qinf1, times=c(0,50))
Oinf1 <- samDataTr(Oinf=Oinf1, grid=grid, result_tr=res_fwd)
oHT <- list(test1 = Oinf1)
result <- FinverseAdjTr(grid=grid, qHT=qHT, oHT=oHT, itermax=3)
}

Todo list:
Currently the function is much slower  than the MC version, and also its convergence is not as good as the MC version.
Should try to speed up the code and improve the convergence.

}
