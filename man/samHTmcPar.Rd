% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inverse.R
\name{samHTmcPar}
\alias{samHTmcPar}
\title{Parallel Monte Carlo forward runs for 2D HT (ensemble)}
\usage{
samHTmcPar(
  domain = c(40, 40, 0, 40, 0, 40),
  grid = NULL,
  TT = 0.1,
  qHT = list(data.frame(Qp = 10, x = 20.5, y = 20.5)),
  oHT = list(data.frame(data = -1, x = 11, y = 11)),
  lrw = 160000,
  ncore = 7
)
}
\arguments{
\item{domain}{a 6-element vector \code{c(nx, ny, x1, x2, y1, y2)} \code{[L]}.}

\item{grid}{grid list from \code{GenGrid()}.}

\item{TT}{\code{n × nsim} matrix of transmissivity realisations \code{[L\eqn{^2}/T]}.}

\item{qHT}{list of pumping test data frames (\code{Qp, x, y}).}

\item{oHT}{list of observation data frames (\code{data, x, y}).}

\item{lrw}{real work array length for \code{steady.2D} (default 160000).}

\item{ncore}{number of CPU cores for parallel execution (default 7).}
}
\value{
An \code{nsim × nobs} matrix of simulated heads at observation wells.
}
\description{
Runs \code{samHT} for each ensemble member in parallel using
\pkg{foreach} + \pkg{doParallel} (\code{\%dopar\%}).  Returns a matrix
where each row corresponds to one ensemble member and columns are observed
heads concatenated across all pumping tests.  Used internally by
\code{\link{Finverse}}.
}
\details{
\strong{Important:} A parallel backend must be registered
(e.g. via \code{doParallel::registerDoParallel(cores = ncore)}) before
calling this function.  \code{Finverse} sets up the backend automatically;
standalone calls need explicit registration.
}
\examples{
data("ht9_synthetic")
grid <- ht9_synthetic$grid
oHT <- ht9_synthetic$oHT
qHT <- ht9_synthetic$qHT
TT= random2d(nsim=50,grid=grid)
TT = TT[,-c(1,2)]
cl <- parallel::makeCluster(10)
doParallel::registerDoParallel(cl)
da = samHTmcPar(grid =grid, TT=TT, qHT= qHT, oHT=oHT,lrw=320000,ncore=10)
parallel::stopCluster(cl)
}
